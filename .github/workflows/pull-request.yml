name: PR Workflows

# Run action on pull request event
on: [pull_request]

jobs:
  # Build exampleSite
  build:
    runs-on: ubuntu-latest
    steps:
      # checkout to the commit that has been pushed
      - uses: actions/checkout@v7

      # Setup mise
      - uses: jdx/mise-action@v4

      - name: Install dependencies
        run: |
          mise run install # install theme dependency

      - name: Build
        run: |
          hugo --minify

  # Check for any broken links in the built site
  markdown-link-check:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      # checkout to the commit that has been pushed
      - uses: actions/checkout@v7

      # Setup mise
      - uses: jdx/mise-action@v4

      - name: Install dependencies
        run: |
          mise run install # install theme dependency

      - name: Build
        run: |
          hugo --minify

      - name: Link Checker
        id: lychee
        uses: lycheeverse/lychee-action@v2.9.0
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
        with:
          fail: false
          args: "--verbose --exclude 'www.udemy.com' './public/**/*.html'"
          output: lychee/out.md

      # Dependabot PRs get a read-only token, so this step cannot post.
      # Do not let that sink the job: the report is also in the job summary.
      - name: Comment Broken Links
        if: ${{ steps.lychee.outputs.exit_code != 0 }}
        continue-on-error: true
        uses: marocchino/sticky-pull-request-comment@v3
        with:
          path: lychee/out.md

      - name: Fail workflow if broken links found
        if: ${{ steps.lychee.outputs.exit_code != 0 }}
        run: exit 1
